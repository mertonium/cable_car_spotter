<h2>New sighting</h2>

<%= render "form.html", changeset: @changeset,
                        cable_cars: @cable_cars,
                        action: sighting_path(@conn, :create) %>

<%= link "Back", to: sighting_path(@conn, :index) %>
